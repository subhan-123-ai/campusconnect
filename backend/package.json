{
  "name": "campusconnect-backend",
  "version": "1.0.0",
  "description": "Backend for CampusConnect - Smart Student Life Platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node seedUniversities.js",
    "seed:admin": "node -e \"require('dotenv').config(); require('./config/db')().then(() => require('./utils/seedAdmin')()).then(() => process.exit(0)).catch(e => { console.error(e); process.exit(1); })\""
  },
  "keywords": [
    "campus",
    "students",
    "collaboration"
  ],
  "author": "Muhammad Subhan",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cloudinary": "^1.40.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.9.5",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "mongodb-memory-server": "^11.2.0",
    "nodemon": "^3.0.1"
  }
}
